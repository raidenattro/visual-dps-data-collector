# 工作日报（2026-07-07）

> 分支 **`try/elbow-extend`**，当日末次功能提交 **`02cb1e3`**。  
> 远端 **`origin/try/elbow-extend`** 停在 **`7a48692`**，本日后续 commit（含本日报）**尚未 push**。

---

## 一、代码提交（已入库）

### 1. 回放预览视频转码与轻量 timeline API（`e1dbf0d`）

- **背景**：高分辨率源视频直接回放卡顿；timeline 全量字段过大。
- **新增**：
  - **`video_transcode.py`**：异步转码为配置高度预览 MP4，进度写入 manifest；
  - **`pose_store.py`**：预览路径 / 转码状态读取；
  - **`api/record_service.py`**：回放视频 URL 优先预览轨。
- **API**：
  - `GET /api/records/{id}/timeline?light=1`：仅 `frame_idx` / `source_frame_idx` / `timestamp_sec`；
  - 沙盒 session 元数据补充 annotation 来源说明。

### 2. 回放性能优化与沙盒标注 UI（`7a48692`）

- **骨架预取**：打开记录后批量拉取全量 pose，减少逐帧请求。
- **渲染**：`requestVideoFrameCallback` 驱动绘制循环；播放态轻量 overlay。
- **转码等待**：预览未就绪时遮罩 + 轮询进度。
- **沙盒 / 复核**：复核/标真筛选共用、加载已有标注跳过默认货架框、微调高对比度货位色、大画布适配。

### 3. docs 按主题整理与 α=0.1 实验报告（`3d13361`）

- **目录**：`hand-probe/`、`alarm-min/`、`segment-filter/`、`features/`、`guides/`；新增 `docs/README.md`、`hand-probe/README.md`。
- **报告**：入库 `hand-probe-ab-alpha01-rtmpose-m`（28 条 · 修订后标注 · α=0.1）及 JSON；`1-1-1/annotation-revision-comparison.md`。
- **脚本**：评估脚本默认 `--out` 指向新子目录；`alarm-min` 报告 SVG 链改为 `../view/`。

### 4. 回放支持 α=0.1 碰撞变体（`02cb1e3`）

- **`api/collision_variants_service.py`**：新增 `hand_ext_0.10` → `timeline_collision_hand_ext_0.10.parquet`。
- **回放 UI**：α 下拉增加 0.1；variant 键显式映射（不再按阈值推断）。
- **批建脚本**：`build_collision_variants.py` 文档同步为 α=0.1/0.2/0.3/0.4。

---

## 二、数据产出（已入库）

| 路径 | 说明 |
|------|------|
| `docs/hand-probe/hand-probe-ab-alpha01-rtmpose-m.md` | 全量 28 条 · α=0.1 A/B（**修订后标注**，161 标真段） |
| `docs/json/hand-probe-ab-alpha01-rtmpose-m.json` | 结构化明细 |
| `docs/hand-probe/1-1-1/annotation-revision-comparison.md` | 1-1-1 标注修订前后 FN/FP 对比 |

α=0.1 实验命令：

```bash
python scripts/data/compare_hand_probe_accuracy.py \
  --tier rtmpose-m --tags "单人,无遮挡" \
  --cameras "1-1-1,1-2-1,2-2-2,2-3-1,2-4-1,2-5-1,2-6-1,2-7-2" \
  --pose-frame-interval 2 --alarm-min 3 --cooldown 0 \
  --extension-ratio 0.1 \
  --out docs/hand-probe/hand-probe-ab-alpha01-rtmpose-m.md
```

---

## 三、本日实验参数与结论（α=0.1）

| 项 | 值 |
|----|-----|
| 样本 | 28 条优质 cohort（rtmpose-m · 单人+无遮挡 · 已复核 · 有标真） |
| 参数 | `interval=2`，`alarm_min=3`，`cooldown=0` |
| 标注版本 | **修订后**（全量标真段 161；较 07-03 旧 cohort 168 少 7 段，主因 clip_0013） |

| 指标 | 手腕 | α=0.1 延长 | Δ |
|------|------|------------|---|
| 漏报 FN | 14 | 14 | 0 |
| 误报 FP | 426 | 438 | +12 |
| Recall | 91.30% | 91.30% | 0 |
| 精确率代理 | 25.65% | 25.13% | −0.52pp |

**结论**：修订后标注下，α=0.1 与手腕 **recall 相同**，误报略增；2-2-2 漏报 2→1（recall 92%→96%）。

---

## 四、待跟进

1. **push 分支**：`3d13361` 起共 3 个 commit 领先 `origin/try/elbow-extend`。
2. **sidecar 补建**：已有记录需重跑 `python scripts/data/build_collision_variants.py` 生成 α=0.1 parquet。
3. **全量重跑**：修订后标注下 α=0.2/0.3/0.4，与 alpha01 同期对比。

---

## 五、相关文档

| 文档 | 说明 |
|------|------|
| [`docs/README.md`](../README.md) | docs 总索引 |
| [`docs/hand-probe/README.md`](../hand-probe/README.md) | 手臂延长 A/B 实验对照 |
| [`docs/hand-probe/hand-probe-ab-alpha01-rtmpose-m.md`](../hand-probe/hand-probe-ab-alpha01-rtmpose-m.md) | 本日 α=0.1 全量报告 |
| [`docs/daily/2026-07-06.md`](2026-07-06.md) | 前日 baseline / infer-collision 对齐 |

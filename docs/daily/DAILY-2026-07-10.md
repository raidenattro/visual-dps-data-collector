# 工作日报（2026-07-10）

> 仓库 **`visual-dps-datacollect`**，分支 **`try/speed-filter`**。  
> 本日主线：**膝踝平均速度前置门控** → **手腕/上肢豁免** → **knee@65 阈值优选与导出** → **关节角/朝向特征组合门控** → **踝点速度门控与 triple90 导出**。  
> 已入库提交 **`2b33697`**（膝踝特征）、**`09c95bf`**（豁免 + 阈值优选）；傍晚至晚间角速度/朝向/踝点实验待本次 commit。

---

## 一、问题背景

| 项 | 内容 |
|----|----|
| 模块 | 前置速度门控（prefilter） |
| 问题 | 下蹲/起身过快时 `lower_mean_speed`（髋+膝+踝）偏高，跳过手腕进框检测导致漏报 |
| 影响 | 同引擎下前置漏报 5 段，含 `clip_0009` 下蹲取货、`clip_0013_42-48` 蹲取等 |
| 环境 | try/speed-filter，28 条 prod-test，local baseline |

## 二、根因分析

| 类型 | 原因 |
|----|----|
| 算法 | 蹲起时髋部（11,12）在画面中位移大，拉高 6 点下肢平均速度 |
| 数据 | 原 `lower_mean_speed` 已是算术平均，非求和；问题在 **髋部纳入门控** 而非聚合方式 |

## 三、实现修改

### 膝踝平均速度前置门控实验

修改文件:

- `event_engine/skeleton_features.py`
- `event_engine/speed_gated_collision.py`
- `scripts/data/validate_prefilter_knee_ankle28.py`（新增）
- `docs/prefilter-knee-ankle-speed-gate.md`（新增）
- `docs/prefilter-knee-ankle-experiment.json`（新增）

修改内容:

- 新增 `KNEE_ANKLE_KPT_INDICES = (13,14,15,16)` 与 `knee_ankle_mean_speed`
- `AggregateVelocitySnapshot` / `IncrementalAggregateVelocityTracker` 同步输出膝踝平均
- `SpeedGateConfig.feature` 支持 `knee_ankle_mean_speed`
- 验证脚本对比两种特征，参数 `pose_frame_interval=2, alarm_min=3, cooldown=0, threshold=60`

## 四、改动文件索引

| 文件路径 | 修改说明 |
|----|----|
| event_engine/skeleton_features.py | 膝踝平均速度特征 |
| event_engine/speed_gated_collision.py | 门控特征路由 |
| scripts/data/validate_prefilter_knee_ankle28.py | 28 条对比验证脚本 |
| docs/prefilter-knee-ankle-speed-gate.md | 实验报告 |
| docs/prefilter-knee-ankle-experiment.json | 原始结果 JSON |

## 五、验证步骤

### 环境

- OS: Windows
- 分支: try/speed-filter

### 执行命令

```
python scripts/data/validate_prefilter_knee_ankle28.py
```

### 结果

- Passed（28/28 记录）

**阈值 60 对比（相对 rule-baseline-local-prod-test）**

| 特征 | TP | FP | FN段 | 召回率 |
|------|-----|-----|------|--------|
| lower_mean_speed（原） | 142 | 287 | 14 | 91.03% |
| knee_ankle_mean_speed（新） | 143 | 286 | 13 | 91.67% |

**重点 clip**

- `clip_0013_42-48`：原漏 109–131 + 159–169 两段；膝踝特征 **恢复 109–131**，159–169 蹲取仍漏
- `clip_0009`、`clip_0020`、`clip_0013_29-53`：膝踝特征未改善

帧级区分度：标真 P50 10.13→8.75，误报 P50 23.16→20.33（膝踝更利于区分）

## 六、性能影响

未测试，需要后续验证。

## 七、Git信息（上午～下午，已入库）

| 项 | 内容 |
|----|----|
| Branch | `try/speed-filter` |
| Commit | `2b33697` 膝踝特征报告 · `09c95bf` 豁免/阈值优选 · `5bdcc4d`/`dc952da` 07-09 日报 |
| Remote status | 未推送 |

## 八、待跟进问题

- 膝踝特征仅回收 1 段漏报，纯蹲取（159–169）仍需水平速度门控或连续帧门控
- 阈值 70 下膝踝特征可达 92.95% 召回（TP=145 FP=310），可作折中方案
- 若采纳膝踝特征，需更新 `export_prefilter_upload.py` 默认 `--feature knee_ankle_mean_speed`

---

## 补充：手腕/上肢低速豁免前置门控（2026-07-10）

### 实现

- `SpeedGateConfig` 增加 `wrist_exempt_max_threshold`、`upper_exempt_max_threshold`
- 逻辑：下肢 `lower_mean_speed > 60` 时，若 `wrist_max_speed` 或 `upper_mean_speed`（肩肘腕平均）≤ 豁免阈，仍做手腕进框检测
- 验证脚本 `validate_prefilter_wrist_exempt28.py`
- 报告 `docs/prefilter-wrist-exempt-speed-gate.md`

### 帧级区分度

| 特征 | 标真 P50 | 误报 P50 |
|------|----------|----------|
| wrist_max_speed | 21.03 | 58.32 |
| upper_mean_speed | 15.13 | 39.67 |

### 结果（28 条，local baseline）

| 方案 | TP | FP | FN段 | 召回率 |
|------|-----|-----|------|--------|
| 无豁免（基线） | 142 | 287 | 14 | 91.03% |
| upper豁免≤60（折中） | 143 | 307 | 13 | 91.67% |
| upper豁免≤100（激进） | 144 | 343 | 12 | 92.31% |

- `clip_0020`、`clip_0013_29-53` 在 upper豁免≤100 下恢复
- `clip_0009`、`clip_0013_42-48` 下蹲段仍漏

---

## 补充：膝踝门控 + 手腕/上肢豁免组合（2026-07-10）

### 实现

- 验证脚本 `validate_prefilter_knee_ankle_wrist_exempt28.py`
- 门控 `knee_ankle_mean_speed ≤ 60` + `wrist/upper` 豁免
- 报告 `docs/prefilter-knee-ankle-wrist-exempt-speed-gate.md`

### 结果（28 条，local baseline）

| 方案 | TP | FP | FN段 | 召回率 |
|------|-----|-----|------|--------|
| 膝踝无豁免 | 143 | 286 | 13 | 91.67% |
| 膝踝 + upper豁免≤60 | 144 | 317 | 12 | 92.31% |
| **膝踝 + upper豁免≤100** | **145** | 349 | **11** | **92.95%** |
| 对照 lower + upper豁免≤100 | 144 | 343 | 12 | 92.31% |

当前前置方案最优：召回 92.95%，仍漏 11 段（含下蹲 clip）。

---

## 补充：膝踝阈值优选 + 豁免重跑（2026-07-10 下午）

### 阶段一

准则「召回≥92%、FP 最小」→ 膝踝阈 **65**（TP=144 FP=298 漏报 12）。

| 档位 | 阈值 | TP | FP | 漏报 | 召回 |
|------|------|-----|-----|------|------|
| 保守≥90% | 50 | 142 | 261 | 14 | 91.03% |
| 主选≥92% | **65** | 144 | 298 | 12 | 92.31% |
| 高召回 | 70 | 145 | 310 | 11 | 92.95% |
| 激进 | 110 | 146 | 349 | 10 | 93.59% |

`docs/prefilter-knee-ankle-threshold-optimal.md`

### 阶段二（膝踝阈 65 + 豁免）

最优：**膝踝65 + upper豁免≤100** → TP=146 FP=352 漏报 10 召回 **93.59%**

- `clip_0009` 在膝踝≥65 即恢复
- 膝踝@110 无豁免：同 TP/漏报，FP=349（更简单）
- `clip_0013_42-48` 蹲取仍漏

`docs/prefilter-knee-ankle-wrist-exempt-speed-gate.md`

---

## 补充：knee@65 前置过滤导出 prod-test（2026-07-10 下午）

### 问题背景

| 项 | 内容 |
|----|------|
| 模块 | `scripts/data/export_prefilter_upload.py` / `localdata/export/` |
| 问题 | 阈值优选结论 knee@65 需落地为可上传导出包，并与 baseline-local、lower@60 prefilter 并列对比 |
| 影响 | 为生产选型提供三方案 FP/FN/召回对照 |
| 环境 | 28 条 local baseline 记录，`pose_frame_interval=2, alarm_min=3, cooldown=0` |

### 根因分析

| 类型 | 原因 |
|----|------|
| 算法 | 膝踝均值特征对下蹲/低速取货更宽松，阈值 65 可在召回≥92% 约束下平衡 FP |
| 数据 | 评估仍以 `rule-baseline-local-prod-test` 为对照基线 |

### 实现修改

#### knee@65 导出

修改文件：

- `localdata/export/rule-speed-prefilter-knee65-prod-test/`（新增目录，28/28）
- `localdata/export/compare_prefilter_triple_baseline_local_prefilter_knee65.md`（三向汇总）
- `localdata/export/compare_rule-speed-prefilter-knee65-prod-test_vs_*.md`（两两对比，评估刷新）

修改内容：

- 执行 `export_prefilter_upload.py --feature knee_ankle_mean_speed --threshold 65`
- manifest 写入 `pose_frame_interval/alarm_min/cooldown` 与 `speed_filter` 配置
- `evaluate_inference_upload.py --in-place` 评估三目录并生成对比 md

修改原因：

- 用户要求导出 knee@65 前置包并给出与 baseline-local、prefilter-prod-test 的对比报告

影响：

- knee@65：TP=144 FP=298 FN=12 召回 92.31%；相对 lower@60 少漏 2 段、FP 多 11

### 改动文件索引

| 文件路径 | 修改说明 |
|----|----|
| `localdata/export/rule-speed-prefilter-knee65-prod-test/` | knee@65 前置导出包 |
| `localdata/export/compare_prefilter_triple_baseline_local_prefilter_knee65.md` | 三向汇总对比 |
| `localdata/export/compare_rule-speed-prefilter-knee65-prod-test_vs_rule-baseline-local-prod-test.md` | knee65 vs baseline |
| `localdata/export/compare_rule-speed-prefilter-knee65-prod-test_vs_rule-speed-prefilter-prod-test.md` | knee65 vs lower@60 |
| `localdata/export/rule-speed-prefilter-knee65-prod-test误报漏报情况.md` | 误报漏报人工分类 |

### 验证步骤

#### 环境

- OS: Windows 10
- Branch: `try/speed-filter`

#### 执行命令

```bash
python scripts/data/export_prefilter_upload.py \
  --baseline-manifest localdata/export/rule-baseline-local-prod-test/_manifest.json \
  --output-dir localdata/export/rule-speed-prefilter-knee65-prod-test \
  --feature knee_ankle_mean_speed --threshold 65 \
  --pose-frame-interval 2 --alarm-min 3 --alarm-cooldown 0
```

#### 结果

- Passed：28/28 导出成功；三目录评估与对比 md 已生成

### 性能影响

未测试，需要后续验证。

### Git信息

| 项 | 内容 |
|----|------|
| Branch | `try/speed-filter` |
| Commit | 导出产物在 `localdata/export/`，见 §十 |
| Remote status | 未推送 |
| Uncommitted files | 见 §九 evening 改动列表 |

### 待跟进问题

- `clip_0013_42-48` 帧 159–169 段 knee@65 仍漏（下蹲相关）
- 是否将生产默认前置改为 knee@65 待决策
- upper/wrist 豁免方案 FP 代价高，暂不推荐

---

## 补充：肩肘腕开合角/角速度门控实验（2026-07-10 傍晚）

### 问题背景

| 项 | 内容 |
|----|------|
| 模块 | `event_engine/skeleton_angles.py` / `validate_prefilter_joint_angle28.py` |
| 问题 | 速度门控难以同时保持高召回与低 FP，需探索肩/肘/腕开合角及角速度特征 |
| 影响 | 评估组合门控能否恢复 prefilter / knee@65 漏报 |
| 环境 | manifest 对齐 `pose_frame_interval=2, alarm_min=3, cooldown=0`，28 条记录 |

### 根因分析

| 类型 | 原因 |
|----|------|
| 算法 | 纯角速度在标真/误报帧 P50 重叠大，无法单独压 FP |
| 数据 | 多数漏报段肘角 P50 较高（伸手/蹲取），可用肘角伸展豁免下肢高速误杀 |

### 实现修改

修改文件：

- `event_engine/skeleton_angles.py`（新增肩/肘/腕开合角与角速度提取）
- `scripts/data/validate_prefilter_joint_angle28.py`（28 条验证 + 组合门控网格）
- `docs/prefilter-joint-angle-speed-gate.md`、`docs/json/prefilter-joint-angle-experiment.json`

修改内容：

- export 抽帧对齐计算关节角；扫描纯角速度门控与「knee@65 + 肘角豁免≥150」组合
- 输出 prefilter / knee@65 漏报段特征画像

影响：

- 最优组合：knee@65 + 肘角豁免≥150 → TP=147 FP=361 FN=9 召回 94.23%（对齐 baseline 召回，FP 仍高于 lower@60）

### 改动文件索引

| 文件路径 | 修改说明 |
|----|----|
| `event_engine/skeleton_angles.py` | 关节角/角速度提取 |
| `scripts/data/validate_prefilter_joint_angle28.py` | 验证脚本 |
| `docs/prefilter-joint-angle-speed-gate.md` | 实验报告 |
| `docs/json/prefilter-joint-angle-experiment.json` | 原始数据 |

### 验证步骤

```bash
python scripts/data/validate_prefilter_joint_angle28.py
```

结果：Passed

### 待跟进问题

- 组合门控 FP=361 仍高于 lower@60（287），需二次压 FP 或分段策略
- 将肘角豁免接入 `SpeedGateConfig` 待实现

---

## 补充：手部朝向 + 肩肘腰身夹角特征（2026-07-10 晚）

### 实现修改

在 `skeleton_angles.py` 新增（export 抽帧对齐）：

- **手腕抬升角** `wrist_elevation_angle_*`：肩→腕相对图像向下方向
- **前臂指向角** `forearm_direction_angle_*`：肘→腕相对向下方向
- **肩-肘躯干夹角** `arm_torso_angle_*`：∠(髋心, 肩, 肘)
- **肘-腰身夹角** `elbow_waist_angle_*`：∠(肩, 肘, 髋心)

### 初步区分度（标真 P50 vs 误报 P50）

| 特征 | 标真 | 误报 | 备注 |
|------|------|------|------|
| `arm_torso_angle_max` | **82.4°** | 70.4° | 取货时肩肘更张开，优于单纯肘角豁免 |
| `wrist_elevation_angle_max` | 45.0° | 45.8° | P50 几乎重叠 |
| `forearm_direction_angle_max` | 61.6° | 85.2° | 误报反而更高，不宜单独作豁免 |

报告已刷新：`docs/prefilter-joint-angle-speed-gate.md` §4.2

---

## 补充：朝向特征 + knee@65/lower@60 组合实验（2026-07-10 晚）

脚本：`scripts/data/validate_prefilter_orientation_combo28.py`

| 方案 | TP | FP | FN | 召回 |
|------|-----|-----|-----|------|
| knee@65 | 144 | 298 | 12 | 92.31% |
| **knee@65 + 躯干70° & 肘150°** | 146 | 324 | **10** | **93.59%** |
| knee@65 + elbow_waist≥145（FP约束优） | 144 | 303 | 12 | 92.31% |
| knee@65 + 肘角豁免≥150 | 147 | 361 | 9 | 94.23% |

改动文件：

- `scripts/data/validate_prefilter_orientation_combo28.py`
- `docs/prefilter-orientation-combo-speed-gate.md`
- `docs/json/prefilter-orientation-combo-experiment.json`

报告：`docs/prefilter-orientation-combo-speed-gate.md`

---

## 补充：多角度 AND/OR 组合实验（2026-07-10 晚）

脚本：`scripts/data/validate_prefilter_multi_angle_logic28.py`

| 逻辑 | 优选方案 | TP | FP | FN |
|------|----------|-----|-----|-----|
| AND（FP约束） | knee@65 + elbow≥160 AND wrist_elev≥60 | 145 | 310 | 11 |
| AND（召回） | knee@65 + arm_torso_mean≥40 AND elbow≥150 | 147 | 341 | 9 |
| OR | _无通过 FP 约束组合_ | — | — | — |

改动文件：

- `scripts/data/validate_prefilter_multi_angle_logic28.py`
- `docs/prefilter-multi-angle-logic-speed-gate.md`
- `docs/json/prefilter-multi-angle-logic-experiment.json`

报告：`docs/prefilter-multi-angle-logic-speed-gate.md`

---

## 补充：knee@65 三重 AND 导出 prod-test（2026-07-10 晚）

脚本：`scripts/data/export_prefilter_triple_angle_upload.py`（新增）

规则：`knee@65 + arm_torso≥90 AND elbow≥150 AND wrist_elev≥60`

| 方案 | TP | FP | FN | 召回 |
|------|-----|-----|-----|------|
| knee@65 | 144 | 298 | 12 | 92.31% |
| **knee@65-triple90** | **145** | 310 | **11** | **92.95%** |

改动文件：

- `scripts/data/export_prefilter_triple_angle_upload.py`
- `localdata/export/rule-speed-prefilter-knee65-triple90-prod-test/`（28/28，未入库）
- `localdata/export/compare_rule-speed-prefilter-knee65-triple90-prod-test_vs_rule-speed-prefilter-knee65-prod-test.md`

对比：`localdata/export/compare_rule-speed-prefilter-knee65-triple90-prod-test_vs_rule-speed-prefilter-knee65-prod-test.md`

---

## 补充：脚部踝点速度阈值选取（2026-07-10 晚）

### 问题背景

| 项 | 内容 |
|----|------|
| 模块 | 前置速度门控 `skeleton_features` / `speed_gated_collision` |
| 问题 | 需基于脚部（踝点）速度独立选取门控阈值，低置信度不参与计算 |
| 影响 | 评估是否可用踝点替代或补充膝踝联合特征 |
| 环境 | 28 条 prod-test，参数对齐 manifest（interval=2, alarm_min=3, cooldown=0） |

### 根因分析

| 类型 | 原因 |
|----|------|
| 算法 | 踝点帧级 P50 标真低于误报，但去掉膝点后段级 FP 压制不足 |
| 数据 | score<0.3 踝点已过滤，部分帧两踝均无效则 fail-open |

### 实现修改

- `event_engine/skeleton_features.py`：`FOOT_ANKLE_INDICES=(15,16)`，输出 `ankle_mean_speed`/`ankle_max_speed`
- `event_engine/speed_gated_collision.py`：`_speed_value` 支持踝点特征
- `scripts/data/validate_prefilter_foot_ankle28.py`：阈网格扫描 + knee@65 对照

### 验证结果

| 方案 | TP | FP | FN | 召回 |
|------|-----|-----|-----|------|
| knee@65 | 144 | 298 | 12 | 92.31% |
| **ankle_max@80** | 144 | **275** | 12 | 92.31% |
| ankle_mean@45 | 142 | 264 | 14 | 91.03% |

报告：`docs/prefilter-foot-ankle-speed-gate.md`，JSON：`docs/json/prefilter-foot-ankle-experiment.json`

改动文件：

- `scripts/data/validate_prefilter_foot_ankle28.py`
- `event_engine/skeleton_features.py`（`FOOT_ANKLE_INDICES`、`ankle_*_speed`）
- `event_engine/speed_gated_collision.py`（踝点特征路由）

### 待跟进

- `ankle_max@80` 与 knee@65 组合门控待验证
- 不建议单独用踝点替代 knee@65

### 导出（2026-07-10）

已导出至 `localdata/export/`：

| 目录 | 阈 | FP | FN | 召回 |
|------|-----|-----|-----|------|
| `rule-speed-prefilter-ankle-mean45-prod-test` | mean@45 | 264 | 14 | 91.03% |
| `rule-speed-prefilter-ankle-max80-prod-test` | max@80 | 275 | 12 | 92.31% |

对比 MD 已生成（vs `rule-baseline-local-prod-test`）。

### ankle_max@80 + triple90（2026-07-10）

规则：将 knee65-triple90 的速度门控替换为 `ankle_max_speed@80` + 三重角度 AND 豁免。

| 方案 | TP | FP | FN | 召回 |
|------|-----|-----|-----|------|
| ankle_max@80 | 144 | 275 | 12 | 92.31% |
| ankle_max@80-triple90 | 145 | 290 | 11 | 92.95% |

目录：`rule-speed-prefilter-ankle-max80-triple90-prod-test`  
对比：`compare_rule-speed-prefilter-ankle-max80-triple90-prod-test_vs_rule-speed-prefilter-ankle-max80-prod-test.md`

---

## 九、本日改动文件汇总（傍晚～晚间，待 commit）

| 文件路径 | 修改说明 |
|----|----|
| `event_engine/skeleton_angles.py` | 肩/肘/腕开合角、角速度、朝向与躯干夹角提取 |
| `event_engine/skeleton_features.py` | 踝点 `ankle_mean_speed` / `ankle_max_speed` |
| `event_engine/speed_gated_collision.py` | 门控路由踝点特征 |
| `scripts/data/validate_prefilter_joint_angle28.py` | 关节角/角速度门控验证 |
| `scripts/data/validate_prefilter_orientation_combo28.py` | 朝向 + 速度组合验证 |
| `scripts/data/validate_prefilter_multi_angle_logic28.py` | 多角度 AND/OR 组合验证 |
| `scripts/data/validate_prefilter_foot_ankle28.py` | 踝点速度阈网格验证 |
| `scripts/data/export_prefilter_triple_angle_upload.py` | knee65-triple90 / ankle-triple90 导出 |
| `docs/prefilter-joint-angle-speed-gate.md` | 关节角实验报告 |
| `docs/prefilter-orientation-combo-speed-gate.md` | 朝向组合实验报告 |
| `docs/prefilter-multi-angle-logic-speed-gate.md` | AND/OR 逻辑实验报告 |
| `docs/prefilter-foot-ankle-speed-gate.md` | 踝点速度实验报告 |
| `docs/json/prefilter-joint-angle-experiment.json` | 关节角原始数据 |
| `docs/json/prefilter-orientation-combo-experiment.json` | 朝向组合原始数据 |
| `docs/json/prefilter-multi-angle-logic-experiment.json` | AND/OR 原始数据 |
| `docs/json/prefilter-foot-ankle-experiment.json` | 踝点实验原始数据 |
| `docs/daily/DAILY-2026-07-10.md` | 本日报 |

## 十、Git 信息（全日本次 commit）

| 项 | 内容 |
|----|----|
| Branch | `try/speed-filter` |
| 本日已入库 | `2b33697` · `09c95bf` · `5bdcc4d` · `dc952da` |
| 本次 commit | `feat(prefilter): 关节角/朝向/踝点门控实验与 07-10 日报`（HEAD） |
| Remote status | 未推送 |
| 未入库 | `localdata/export/` 导出包与对比 MD（本地评估产物） |

## 十一、待跟进问题（汇总）

1. `clip_0013_42-48` 帧 159–169 下蹲段：knee@65 / 角速度豁免均未完全恢复
2. 生产默认前置：knee@65（92.31%）vs ankle_max@80（FP 更低 275）vs triple90（92.95%）待决策
3. 肘角豁免≥150 召回对齐 baseline 但 FP=361 过高；朝向组合「躯干70° & 肘150°」FN=10 召回 93.59% 可作折中
4. `SpeedGateConfig` 接入角度豁免与 triple AND 逻辑待工程化
5. `ankle_max@80` 与 knee@65 速度特征组合门控待验证

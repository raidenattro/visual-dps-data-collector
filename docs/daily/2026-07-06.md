# 工作日报（2026-07-06）

> 分支 **`try/elbow-extend`**，当日 HEAD **`e58560f`**（`e58560ff8b77dbf1bea4c6c6284a0d8c8bb0036b`）。  
> 远端 **`origin/try/elbow-extend`** 仍停在 **`205751f`**，本日 6 个 commit **尚未 push**。  
> 关联仓库 **ShelfPickSense**（`infer-collision` / `data28-merged` 对比）；本仓库为采集与 baseline 导出侧，**未改 ShelfPickSense 源码**。

---

## 一、代码提交（已入库）

### 1. infer-collision 引擎与 ShelfPickSense 对齐（`d3e5564`）

- **背景**：`export_rule_baseline_frames` 与 ShelfPickSense `infer-collision` 逐帧结果不一致，需统一碰撞逻辑与帧枚举。
- **新增**：
  - **`event_engine/collision_infer.py`**：移植 box_human_det 纯手腕 + cv2 碰撞处理器；
  - **`event_engine/shelf_picksense_align.py`**：复刻 ShelfPickSense 的 `resolve_infer_frame_size`、货框缩放（`build_collision_boxes`）；
  - **`event_engine/collision_sim.py`**：`simulate_frame_events_infer_collision`（skeleton min~max、跳帧、缺帧占位）。
- **修改**：`event_engine/box_identity.py`（`shelf_box_collision_token`）、`annotation_boxes.py`（`orig_contour` float32）。

### 2. baseline 导出对齐 infer-collision（`179ca6a`）

- **`scripts/data/export_rule_baseline_frames.py`**：改用 `InferCollisionProcessor` + `shelf_picksense_align`；
- **缺帧行为**：与 ShelfPickSense 一致，**缺帧不调用** `processor.process()`，避免状态机误推进；
- **新增 CLI**：`--infer-size-record-root`、`--data28-manifest`，按 data28 `record_XXX` 目录解析 infer 尺寸（无 manifest 时 853×480，与 infer-collision 一致）。

### 3. data28 合并标注导出与单货架对比（`a0c31a3`）

- 新增 **`export_shelf_picksense_data28_merged.py`**：28 条记录导出至 ShelfPickSense 目录，`annotation.json` 强制 reflection **合并标注**；
- 新增 **`compare_shelf81_jsonl.py`**：81 单标注重算 vs `output.jsonl`（验证 jsonl 与单货架一致，差异 0）；该脚本已在 `5288b80` 删除。

### 4. 评估脚本公共模块（`0a8c139`）

- **`scripts/data/eval_dataset.py`**：优质 28 条样本筛选（标签/机位/复核）、段重叠判定、误报帧构建；
- **`event_engine/cv2_shim.py`**：统一 `ensure_cv2_point_polygon_test()` 兜底。

### 5. scripts/data 整理（`5288b80`）

- **合并**：`export_shelf_picksense_demo` + `export_shelf_picksense_data28_merged` → **`export_shelf_picksense.py`**（`--layout demo|data28-merged`）；
- **删除**：`compare_shelf81_jsonl.py`；
- **归档**：`migrate_pose_model_tiers.py`、`migrate_event_review_to_review_dir.py` → `scripts/archive/data/`；
- **改 import**：8 个评估/导出脚本不再从 `analyze_wrist_feature_discrimination` 引私有函数。

### 6. 文档更新（`e58560f`）

- **`scripts/README.md`**：脚本索引、公共模块说明；
- **`docs/migrate-event-review.md`**、**`README.md`**：迁移脚本路径改为 `scripts/archive/data/`。

---

## 二、数据产出（未入库，本地）

| 路径 | 说明 |
|------|------|
| `ShelfPickSense/data/data28-merged/Train/record_001` … `record_028` | 合并标注 + skeleton + event_review |
| `localdata/export/rule-baseline-prod/` | 28 条 baseline 逐帧 JSON + `_manifest.json` |
| `localdata/export/rule-baseline-prod-test/` | 与 prod **内容相同**（联调副本，可删其一） |

导出命令（项目根目录）：

```bash
# ShelfPickSense 记录包（合并标注）
python scripts/data/export_shelf_picksense.py --layout data28-merged --clean

# baseline（与 infer-collision 对比时必须带 infer-size-record-root）
python scripts/data/export_rule_baseline_frames.py \
  --out-dir localdata/export/rule-baseline-prod \
  --infer-size-record-root D:/work/workspace/git-repo/ShelfPickSense/data/data28-merged/Train \
  --pose-frame-interval 2 --alarm-min 3 --cooldown 0
```

ShelfPickSense 侧：

```bash
python main.py infer-collision \
  --record-dir data/data28-merged/Train/record_001 \
  --output outputs/output.jsonl \
  --pose-frame-interval 2
```

---

## 三、本日联调参数

| 项 | 值 |
|----|-----|
| 样本 | data28 优质 28 条（rtmpose-m · 单人+无遮挡 · 已复核 · 有标真） |
| 代表 clip | `clip_0013_start_00-11-22_rtmpose_m` / `record_001` |
| `pose_frame_interval` | 2 |
| `alarm_min_consecutive_frames` | 3 |
| `alarm_cooldown_frames` | 0 |
| 标注 | reflection **合并标注**（MAP_6 等）；非 data28 原单货架 81 |
| infer 尺寸（data28-merged 无 manifest） | **853×480**（annotation_size 1280×720 推算） |

---

## 四、联调现象与结论

| 现象 | 根因 / 结论 | 处理 |
|------|-------------|------|
| export 4552 帧 vs jsonl 4120 帧 | 旧 export 按 timeline 全长枚举 | **已修**：skeleton min~max（`d3e5564`） |
| 帧数一致仍碰撞/告警不一致 | 缺帧推进状态机、infer 尺寸 852 vs 853、标注单货架 vs 合并 | **已修**缺帧 + infer 映射（`179ca6a`）；合并标注导出（`a0c31a3`） |
| 81 单标注 vs jsonl | jsonl 用 data28 包内单货架 | **差异 0**（验证用，非生产路径） |
| 合并标注 export vs jsonl | 标注 polygon 不同 | **预期**；对齐后 clip_0013 **差异 0** |
| `picking_prob` / `predicted_box_tokens` 不一致 | export 侧为空，jsonl 有占位 | 不影响碰撞/告警对比 |

### 4.1 clip_0013 对齐验收（合并标注 + infer-size-record-root）

对比 `outputs/output.jsonl` vs `rule-baseline-prod/clip_0013_start_00-11-22_rtmpose_m.json`：

| 指标 | 对齐前 | 对齐后 |
|------|--------|--------|
| 帧数 | 4120 / 4120 | 4120 / 4120 |
| picking 帧数 | 444 / 460 | **444 / 444** |
| is_picking 差异 | 24 帧 | **0** |
| collision 差异 | ~30 帧 | **0** |
| alarm 差异 | ~30 帧 | **0** |

diff 工具：`ShelfPickSense/scripts/cmp_det_service.py`。

---

## 五、问题排查命令（备忘）

```bash
# 本仓库：单条 baseline 试导出
python scripts/data/export_rule_baseline_frames.py --dry-run

# infer 尺寸：源记录 manifest vs data28-merged 无 manifest
python -c "
from pathlib import Path
from scripts.data.eval_dataset import *
from event_engine.shelf_picksense_align import resolve_infer_frame_size
from event_engine.annotation_boxes import load_annotation_config
from api.record_service import locate_record_by_id
from api.accuracy_service import resolve_annotation_for_accuracy_record
from config_loader import resolve_app_paths, resolve_config_path
resolve_config_path(None)
loc = locate_record_by_id('rtmpose-m/1-1-1/clip_0013_start_00-11-22_rtmpose_m')
ann = load_annotation_config(resolve_annotation_for_accuracy_record(resolve_app_paths(), loc, pose_tier='rtmpose-m'))
merged = Path(r'D:/work/workspace/git-repo/ShelfPickSense/data/data28-merged/Train/record_001')
print('source', resolve_infer_frame_size(loc.path, ann))
print('merged', resolve_infer_frame_size(merged, ann))
"

# ShelfPickSense：jsonl vs baseline
cd D:/work/workspace/git-repo/ShelfPickSense
python scripts/cmp_det_service.py outputs/output.jsonl \
  D:/work/workspace/git-repo/visual-dps-datacollect/localdata/export/rule-baseline-prod/clip_0013_start_00-11-22_rtmpose_m.json
```

---

## 六、待跟进

1. **push 分支**：`try/elbow-extend` 本地 `e58560f`，领先 `origin/try/elbow-extend`（`205751f`）6 个 commit。
2. **全量 diff**：ShelfPickSense 对 28 条 record 批量 `infer-collision`，逐条对比 `rule-baseline-prod` JSON。
3. **目录去重**：`rule-baseline-prod` 与 `rule-baseline-prod-test` 保留其一。
4. **可选重构**：合并 `compare_hand_probe_accuracy` 与 `compare_alarm_threshold_accuracy` 为单一 CLI。

---

## 七、相关文档

| 文档 | 说明 |
|------|------|
| [`scripts/README.md`](../../scripts/README.md) | 脚本索引（含 `eval_dataset`、`export_shelf_picksense`） |
| [`docs/migrate-event-review.md`](../migrate-event-review.md) | event_review 迁移（脚本已归档） |
| `ShelfPickSense/data/data28/manifest.json` | 28 条 record 与 source_record_id 映射 |
| 分支 **`try/elbow-extend`** @ **`205751f`** 之前 | 手臂延长探针 A/B、碰撞沙盒等（非本日 baseline 主线） |

# Windows GPU 推理环境（ONNX Runtime CUDA 12 + cuDNN 9）
#
# 推荐：powershell -ExecutionPolicy Bypass -File scripts/setup/setup_windows.ps1
# 手动：
#   pip install -r requirements-windows-gpu.txt
#   pip install "rtmlib>=0.0.13" --no-deps

-r requirements-base.txt

# 勿与 onnxruntime（CPU 包）共存
onnxruntime-gpu>=1.20.0

# ORT CUDA 需 cuDNN 9 DLL；运行时由 ort_cuda_setup.py 加入 PATH / add_dll_directory
nvidia-cudnn-cu12>=9.1.0

# Bundled ffmpeg executable for H.264 fast-start playback previews.
imageio-ffmpeg>=0.5.1,<0.7
